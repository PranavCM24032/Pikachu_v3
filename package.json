{
  "name": "pykachu-hunt",
  "version": "3.0.0",
  "private": true,
  "description": "Pykachu Hunt - decode the code. capture pikachu.",
  "scripts": {
    "build": "tailwindcss -i css/tailwind-input.css -o css/tailwind.css --minify && node build.js",
    "serve": "npx serve -s ."
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "tailwindcss": "^3.4.0"
  }
}
